import { View, Text } from "react-native";
import React from "react";

const useGetBearerKey = () => {
  return (
    <View>
      <Text>useGetBearerKey</Text>
    </View>
  );
};

export default useGetBearerKey;
